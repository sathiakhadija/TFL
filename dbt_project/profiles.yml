tfl_pipeline:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'tfl_user') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'tfl_password') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'tfl_pipeline') }}"
      schema: public
      threads: 4
